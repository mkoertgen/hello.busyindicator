<UserControl x:Class="hello.busyindicator.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:busyindicator="clr-namespace:hello.busyindicator"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance busyindicator:INotificationsViewModel}">
    <Expander IsExpanded="{Binding ItemsVisible}">
        <Expander.Header>
            <TextBlock x:Name="LastItem"/>
        </Expander.Header>
        <ListView x:Name="Items" 
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.CanContentScroll="False"/>
    </Expander>
</UserControl>
